#pragma once

#include <cstdint>

// ============================================================================
// API проверки видимости. Любой модуль подключает этот заголовок и спрашивает:
// "виден ли мне этот враг?" — получает true/false.
//
// Сейчас бэкенд: crosshair-ID (m_iIDEntIndex) — враг считается видимым, когда он
// под прицелом на модели. Надёжно и безопасно, но требует навести на тело.
//
// TODO(trace): полноценная трассировка луча (eye -> точка врага) через игровую
// функцию sub_1809913A0 + GameTraceManager (off_1820459C0). Тогда видимость
// будет определяться в ЛЮБОЙ точке FOV, а не только под прицелом. Реализуется
// в этот же API (SEH-обёрнуто), внешний интерфейс не меняется.
// ============================================================================

namespace Nemesis::Visibility
{
    // Виден ли локальному игроку враг с индексом пешки targetPawnIndex.
    bool VisibleToLocal(std::uintptr_t clientBase, std::uintptr_t localPawn, int targetPawnIndex);
}
