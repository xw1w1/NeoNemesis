#pragma once

// ============================================================================
// Конфиг легит-бота. Меняй значения и пересобирай.
// ============================================================================
namespace Nemesis::LegitBot::Config
{
    // Куда целиться (точки создаются сами):
    inline constexpr bool  aimHead = true;    // голова
    inline constexpr bool  aimBody = false;   // тело (торс) — включи = true

    // Круг FOV (радиус в пикселях): цель берётся только внутри него
    inline constexpr float fovRadius = 90.0f;

    // Сила магнита мыши к цели (0.1 = слабо/плавно, 0.6 = сильно/резко).
    // Двигает реальную мышь к точке — тянет прицел в цель.
    inline constexpr float aimSpeed = 0.35f;

    // Порог авто-огня (px): стреляет только когда прицел так близко к точке
    // И игра подтверждает, что цель видна (не сквозь стену).
    inline constexpr float fireDist = 2.5f;
}
